---
import { socialMedia } from "~/data/config";

// For now, using reviews from JSON. Later can be replaced with Facebook API
import reviewsData from "~/data/reviews.json";

// Use Facebook page URL directly
const facebookPageUrl = socialMedia.facebook.url;
---

<div class="facebook-reviews-wrapper">
	<div class="swiper facebook-reviews-swiper">
		<div class="swiper-wrapper">
			{reviewsData.map((review) => (
				<div class="swiper-slide">
					<div class="px-4 lg:px-8 py-8">
						<div class="bg-white rounded-lg shadow-md p-6 lg:p-8 text-center">
							<div class="flex items-center justify-center mb-4">
								<svg class="w-8 h-8 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
									<path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
								</svg>
							</div>
							<blockquote class="border-none">
								<p class="text-lg lg:text-xl font-medium text-gray-700 text-balance mb-4">
									"{review.review}"
								</p>
								<footer class="text-center">
									<cite class="font-semibold text-brand-red-dark not-italic">{review.author}</cite>
								</footer>
							</blockquote>
						</div>
					</div>
				</div>
			))}
		</div>
		<div class="swiper-pagination"></div>
	</div>
	
	<div class="flex justify-center mt-8 relative z-10">
		<a
			href={facebookPageUrl}
			target="_blank"
			rel="noopener noreferrer"
			class="button button-green flex items-center gap-2 relative z-10 pointer-events-auto"
		>
			<svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
				<path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
			</svg>
			Trimite mesaj pe Facebook
		</a>
	</div>
</div>

<script>
	import Swiper from "swiper";
	import { Pagination, Autoplay } from "swiper/modules";
	import "swiper/css";
	import "swiper/css/pagination";

	document.addEventListener("astro:page-load", () => {
		const reviewsSwiper = document.querySelector(".facebook-reviews-swiper") as HTMLElement;
		if (reviewsSwiper) {
			new Swiper(reviewsSwiper, {
				modules: [Pagination, Autoplay],
				loop: true,
				slidesPerView: 1,
				spaceBetween: 32,
				autoplay: {
					delay: 4000,
					disableOnInteraction: false,
				},
				pagination: {
					el: ".swiper-pagination",
					clickable: true,
				},
				breakpoints: {
					640: {
						slidesPerView: 1,
						spaceBetween: 40,
					},
					1024: {
						slidesPerView: 2,
						spaceBetween: 48,
					},
				},
			});
		}
	});
</script>

<style>
	.facebook-reviews-wrapper {
		max-width: 1200px;
		margin: 0 auto;
	}

	.facebook-reviews-swiper .swiper-pagination-bullet {
		background: var(--color-brand-red-dark);
		opacity: 0.5;
	}

	.facebook-reviews-swiper .swiper-pagination-bullet-active {
		opacity: 1;
		background: var(--color-brand-red-dark);
	}

	/* Ensure button is fully clickable */
	.facebook-reviews-wrapper > div:last-child {
		position: relative;
		z-index: 10;
	}

	.facebook-reviews-wrapper > div:last-child a {
		display: inline-flex;
		position: relative;
		z-index: 10;
		pointer-events: auto;
		width: auto;
		min-width: fit-content;
	}
</style>

